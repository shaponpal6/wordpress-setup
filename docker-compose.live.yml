version: '3.8'

services:
  # Live MariaDB Database
  live-db:
    image: mariadb:11.4.9
    container_name: live_wordpress_db
    restart: unless-stopped
    env_file: .env
    environment:
      - MYSQL_DATABASE=${LIVE_DB_NAME}
      - MYSQL_USER=${LIVE_DB_USER}
      - MYSQL_PASSWORD=${LIVE_DB_PASSWORD}
      - MYSQL_ROOT_PASSWORD=${LIVE_DB_ROOT_PASSWORD}
    volumes:
      - live_db_data:/var/lib/mysql
      - /etc/wordpress/live/config/mariadb:/etc/mysql/conf.d
      - /var/log/wordpress/mysql:/var/log/mysql
    networks:
      - live_network
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  # Live PHP-FPM with PHP 7.4.33
  live-php-fpm:
    build:
      context: .
      dockerfile: Dockerfile.php
    container_name: live_wordpress_php
    restart: unless-stopped
    env_file: .env
    environment:
      - WORDPRESS_DB_HOST=live-db:3306
      - WORDPRESS_DB_NAME=${LIVE_DB_NAME}
      - WORDPRESS_DB_USER=${LIVE_DB_USER}
      - WORDPRESS_DB_PASSWORD=${LIVE_DB_PASSWORD}
    volumes:
      - live_wordpress_data:/var/www/html
      - /var/www/wordpress-live/wp-content:/var/www/html/wp-content
      - /etc/wordpress/live/config/php/php.ini:/usr/local/etc/php/conf.d/custom.ini
      - /etc/wordpress/live/config/php/www.conf:/usr/local/etc/php-fpm.d/www.conf
      - /var/log/wordpress/live:/var/log/php
    networks:
      - live_network
    security_opt:
      - no-new-privileges:true
    depends_on:
      - live-db
    healthcheck:
      test: ["CMD", "php", "-r", "echo 'PHP-FPM is running';"]
      timeout: 20s
      retries: 10

  # Live phpMyAdmin
  live-phpmyadmin:
    image: phpmyadmin:latest
    container_name: live_phpmyadmin
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:80"  # Bind to localhost only for security
    environment:
      - PMA_HOST=live-db
      - PMA_USER=${LIVE_DB_USER}
      - PMA_PASSWORD=${LIVE_DB_PASSWORD}
      - MYSQL_ROOT_PASSWORD=${LIVE_DB_ROOT_PASSWORD}
      - PMA_ABSOLUTE_URI=http://199.19.74.239:8081  # Specify absolute URI for security
    networks:
      - live_network
    depends_on:
      - live-db
    security_opt:
      - no-new-privileges:true

volumes:
  live_db_data:
    name: live_wordpress_db_data
  live_wordpress_data:
    name: live_wordpress_html_data

networks:
  live_network:
    name: live_wordpress_network
    external: true